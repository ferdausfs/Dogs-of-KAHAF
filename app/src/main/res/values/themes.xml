<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!--
      v2.1.8: moved the `xmlns:tools` declaration to the <resources> root so
      every <item> can use `tools:ignore` cleanly. Previously the namespace
      was redeclared on a single <item> which works but trips lint warnings.
    -->
    <style name="Theme.GuardianShield" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:ignore="NewApi">true</item>
    </style>

    <style name="Theme.GuardianShield.Overlay" parent="android:Theme.Material.NoActionBar.TranslucentDecor">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
