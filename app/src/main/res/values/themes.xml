<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.GuardianShield" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/shield_blue</item>
        <item name="colorSecondary">@color/safety_green</item>
        <item name="colorTertiary">@color/islamic_gold</item>
        <item name="colorError">@color/alert_red</item>
        <item name="android:windowBackground">@color/grey_50</item>
    </style>

    <!-- Splash theme for MainActivity until content loads -->
    <style name="Theme.GuardianShield.Splash" parent="Theme.GuardianShield" />

    <!-- Block overlay — translucent dark -->
    <style name="Theme.GuardianShield.Overlay" parent="Theme.Material3.DayNight">
        <item name="android:windowBackground">@color/scroll_bg</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <!-- Scroll suggestion — Islamic dark navy + gold accent -->
    <style name="Theme.GuardianShield.Scroll" parent="Theme.Material3.DayNight">
        <item name="android:windowBackground">@color/scroll_bg</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
