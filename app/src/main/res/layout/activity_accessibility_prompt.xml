<?xml version="1.0" encoding="utf-8"?>
<!-- ONE SHIELD (One UI 8, color rev r2) — urgent accessibility prompt.
     Was: WATCHTOWER V3 (red halo + shield-off icon + danger CTA).
     Now matches guardian-redesign/mocks/oneui8/accessibility.html: no halo,
     the ⚠️ title IS the visual (copy verbatim incl. ⚠️), 15sp body, single
     accent Enable action in the thumb zone, mono path note.
     Binding preserved: btnEnable (destination unchanged). -->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/overlay_dim_solid">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:paddingHorizontal="28dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/prompt_accessibility_title"
            android:textColor="@color/on_surface"
            android:textSize="24sp" android:textStyle="bold" android:letterSpacing="-0.02"
            android:gravity="center"
            android:lineSpacingMultiplier="1.25"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="14dp"
            android:maxWidth="310dp"
            android:text="@string/prompt_accessibility_body"
            android:textColor="@color/on_surface_variant"
            android:textSize="15sp" android:lineSpacingMultiplier="1.6"
            android:gravity="center"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnEnable"
            style="@style/Widget.GuardianShield.Button"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:layout_marginTop="28dp"
            android:text="@string/prompt_accessibility_btn"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="@string/prompt_accessibility_note"
            android:fontFamily="monospace"
            android:textColor="@color/on_surface_dim"
            android:textSize="11sp"/>
    </LinearLayout>
</FrameLayout>
