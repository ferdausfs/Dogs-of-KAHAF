<?xml version="1.0" encoding="utf-8"?>
<!-- WATCHTOWER V3 — onboarding page. Bindings preserved: iconFrame txtIcon
     txtHighlight txtTitle txtBody (emoji glyph renders inside a beacon halo). -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:paddingHorizontal="30dp">

    <FrameLayout
        android:id="@+id/iconFrame"
        android:layout_width="104dp"
        android:layout_height="104dp"
        android:background="@drawable/bg_halo_beacon">
        <TextView
            android:id="@+id/txtIcon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:textSize="40sp"/>
    </FrameLayout>

    <TextView
        android:id="@+id/txtHighlight"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:textColor="@color/primary"
        android:textSize="11.5sp" android:textStyle="bold"
        android:letterSpacing="0.12" android:textAllCaps="true"
        android:gravity="center"/>

    <TextView
        android:id="@+id/txtTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:textColor="@color/on_surface"
        android:textSize="26sp" android:textStyle="bold" android:letterSpacing="-0.02"
        android:gravity="center"/>

    <TextView
        android:id="@+id/txtBody"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="14dp"
        android:textColor="@color/on_surface_variant"
        android:textSize="14sp" android:lineSpacingMultiplier="1.65"
        android:gravity="center"/>
</LinearLayout>
