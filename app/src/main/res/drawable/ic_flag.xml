<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="@color/warning_amber">
    <!-- Material outlined flag — reused as the "report this block" action icon.
         Tinted with the app's existing warning_amber accent (colors.xml), the
         same color used for the temp-block banner / secondary warning chrome. -->
    <path
        android:fillColor="@android:color/white"
        android:pathData="M14.4,6L14,4H5v17h2v-7h5.6l0.4,2h7V6z"/>
</vector>
